1.Creating your project directory
2.creating a virtual environment<pipenv>: cmd pipenv shell 3. install fastapi[standard]: cmd pipenv install "fastapi[standard]"
4.creating application code. 5. running the server. pipenv run fastapi dev <name of file>
